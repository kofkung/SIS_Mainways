Options -Indexes

<FilesMatch "^(sis-contact-config\.example\.php|.*\.(md|json|lock))$">
  Require all denied
</FilesMatch>

<IfModule mod_headers.c>
  Header always set Cache-Control "no-store, max-age=0"
  Header always set X-Content-Type-Options "nosniff"
</IfModule>
